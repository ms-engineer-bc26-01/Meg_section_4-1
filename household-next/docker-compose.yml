version: '3.8'

services:
  app:
    build: .
    ports:
      - '3000:3000'
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    environment:
      - NODE_ENV=development
      # Docker コンテナ内でのファイル変更検知 (ホットリロード対応: 3-1 要件)
      - WATCHPACK_POLLING=true
    tty: true
